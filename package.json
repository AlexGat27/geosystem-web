{
    "name": "GeosystemApp",
    "version": "1.0.0",
    "description": "My Geosystem Application",
    "scripts": {
        "start": "concurrently \"npm run start --prefix client-app\" \"npm run server --prefix web-server\"",
        "install-all": "npm install --prefix client-app && npm install --prefix web-server"
    },
    "dependencies": {},
    "devDependencies": {
        "concurrently": "^6.1.0"
    }
}
